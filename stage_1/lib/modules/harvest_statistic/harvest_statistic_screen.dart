import 'package:flutter/material.dart';

class HarvestStatisticScreen extends StatelessWidget {
  const HarvestStatisticScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Harvest Statistic Screen'),
      ),
    );
  }
}